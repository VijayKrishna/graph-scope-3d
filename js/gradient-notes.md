```
				// var segments = 2;
				// var material = new THREE.LineBasicMaterial( { vertexColors: THREE.VertexColors } );
				// var positions = [];
				// var colors = [];
				// var r = 500	;
				// for ( var i = 0; i < segments; i ++ ) {
				// 	var x = Math.random() * r - r / 2;
				// 	var y = Math.random() * r - r / 2;
				// 	var z = Math.random() * r - r / 2;
				// 	// positions
				// 	positions.push( x, y, z );
				// 	// colors
				// 	colors.push( ( x / r ) + 0.5 );
				// 	colors.push( ( y / r ) + 0.5 );
				// 	colors.push( ( z / r ) + 0.5 );
				// }
				// var bufferedPositions = new THREE.Float32BufferAttribute( positions, 3 );
				// geometry.addAttribute( 'position',  bufferedPositions);
				// var bufferedColors = new THREE.Float32BufferAttribute( colors, 3 );
				// geometry.addAttribute( 'color', bufferedColors );
				// geometry.computeBoundingSphere();
				// var mesh = new THREE.Line( geometry, material );
				// env.scene.add( mesh );
```